import React from "react";

function Square() {
  //! PROPS FROM BOARD
  // makeAMove function

  return (
    <div>
      Square
      {
        // render an 'X' or an 'O'
      }
    </div>
  );
}

export default Square;
